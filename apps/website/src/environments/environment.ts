// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseProjectId: 'territory-offline-site',
  firebaseApiKey: 'AIzaSyDAqpnyqk3M8j_qQw0uPNerSp_3X9-q--4',
  firebaseAuthDomain: 'territory-offline-site.firebaseapp.com',
  firestoreURL: 'https://territory-offline-site.firebaseio.com/',
  firebaseStorageBucket: 'territory-offline-site.appspot.com',

  releasesHost: 'https://theocratic.blob.core.windows.net/to-installations',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
